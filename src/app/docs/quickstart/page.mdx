export const metadata = {
  title: 'Quickstart — your first paid agent call in 5 minutes',
  description: 'n-payment SDK only. Copy-paste five blocks.',
};

# Quickstart — 5 min

> Goal: at the end of this page you have a running paywall and an agent that pays $0.01 USDC to call it.
> Built on [`n-payment@0.29.1`](https://www.npmjs.com/package/n-payment) — single peer dep on `viem`. Open-source · MIT.

## Step 1 — install (20 sec)

<CodeBlock language="bash" code={`mkdir my-paid-agent && cd my-paid-agent
npm init -y
npm install n-payment viem hono`} />

## Step 2 — provider side: paywall + tool (90 sec)

<CodeBlock language="ts" caption="server.ts" code={`import { createPaywall } from 'n-payment/middleware';
import { Hono } from 'hono';

const app = new Hono();

app.use(
  '*',
  createPaywall({
    payTo: process.env.PAY_TO as \`0x\${string}\`,
    chain: 'goat-mainnet',
    asset: 'USDC',
    routes: {
      '/api/forecast': { priceMicroUsdc: 10_000n },   // $0.01
    },
  }),
);

app.get('/api/forecast', (c) => c.json({ city: 'Hanoi', temp: 31 }));

export default app;`} />

Verify the 402 contract:

<CodeBlock language="bash" code={`curl -i http://localhost:3000/api/forecast
# → HTTP/1.1 402 Payment Required
# → WWW-Authenticate: x402-USDC realm="…", price="10000", chain="goat-mainnet"`} />

## Step 3 — consumer side: pay (60 sec)

<CodeBlock language="ts" caption="agent.ts" code={`import { createPaymentClient } from 'n-payment';

const client = createPaymentClient({
  chains: ['goat-mainnet'],
  ows:    { wallet: 'my-agent' },
  policy: { maxPerTransaction: 100_000n },
});

const res = await client.fetchWithPayment('http://localhost:3000/api/forecast');
console.log(res.status, await res.json());
// → 200 { city: 'Hanoi', temp: 31 }`} />

The SDK handles the 402 → sign → retry round-trip in one call. Keys never leave the OWS vault.

## Step 4 — paid MCP server (60 sec)

Want to expose your endpoint as an MCP tool that any agent runtime can call?

<CodeBlock language="ts" caption="paid-mcp.ts" code={`import { createPaidMcpServer, paidTool } from 'n-payment/agent';

const server = createPaidMcpServer({
  name:  'MyAgentService',
  payTo: '0xYourAddress',
  chain: 'goat-mainnet',
  tools: [
    paidTool({
      name:        'forecast',
      description: 'Get weather forecast',
      price:        10_000n,           // $0.01 USDC
      handler:      async ({ city }) => ({ city, temp: 22 }),
    }),
  ],
});

await server.listen(3000);`} />

## Step 5 — register on the bundle catalog (30 sec)

Skip the boilerplate: pre-built HyperMove bundles cover the common shapes (x402-only, multi-chain, BTC-as-treasury, paid MCP server).

1. Open [`/portal`](/portal).
2. Pick a bundle.
3. Submit your email — we send the bundle ZIP within minutes.

## You're done

- ✅ Real `HTTP 402` paywall via `n-payment/middleware`
- ✅ `fetchWithPayment()` that pays $0.01 USDC
- ✅ Vault-bound signing — your agent never touches a private key
- ✅ 27 chains · 14 protocols — see [`/docs/n-payment`](/docs/n-payment)
