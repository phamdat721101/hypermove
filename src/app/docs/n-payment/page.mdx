export const metadata = {
  title: 'n-payment — agent payments in 5 lines',
  description: 'fetchWithPayment(), createPaywall(), 27 chains, 14 protocols. MIT-licensed.',
};

# n-payment

> **`n-payment@0.29.1`** on npm — collapses x402 + ERC-8004 + BTC lending + OWS signing into one call.
> Single peer dependency: `viem`. Dual CJS/ESM output. MIT-licensed.

## Three principles

1. **Agents never touch a private key.** OWS vault holds keys and enforces per-tx policy.
2. **BTC is the treasury; USDC is borrowed just-in-time.** Lock PegBTC → borrow → pay → repay → unlock.
3. **Five lines, not five hundred.** `fetchWithPayment()` unifies the lifecycle.

## Consumer-side — `fetchWithPayment()`

<CodeBlock language="ts" caption="agent.ts" code={`import { createPaymentClient } from 'n-payment';

const client = createPaymentClient({
  chains: ['goat-mainnet', 'base-mainnet'],
  ows:    { wallet: 'my-agent' },
  policy: {
    maxPerTransaction: 100_000n,         // $0.10 cap per call
    maxPerDay:        10_000_000n,       // $10.00 daily cap
    treasury:         'BTC',             // PegBTC-collateralized
    rateLimit:        { maxRequests: 100, windowMs: 60_000 },
  },
});

const res = await client.fetchWithPayment('https://hypermove.dev/api/paid-endpoint');
console.log(await res.json());           // 200 OK, paid in ~1.2s`} />

Under the hood:

1. `GET` the target URL → server returns `HTTP 402 + WWW-Authenticate: x402-USDC`.
2. Vault checks policy (spend cap, chain allowlist, token allowlist).
3. If `treasury: 'BTC'`, the lending adapter locks PegBTC + borrows the exact USDC.
4. EIP-3009 authorize signature is produced inside the OWS vault. The agent never sees the key.
5. The client retries with `X-Payment` header → server returns `200`.

## Provider-side — `createPaywall()`

<CodeBlock language="ts" caption="server.ts" code={`import { createPaywall } from 'n-payment/middleware';
import { Hono } from 'hono';

const app = new Hono();
const paywall = createPaywall({
  payTo: '0xYourAddress',
  chain: 'goat-mainnet',
  asset: 'USDC',
  routes: {
    '/api/paid-endpoint': { priceMicroUsdc: 10_000n },   // $0.01 / call
  },
});

app.use('*', paywall);
app.get('/api/paid-endpoint', (c) => c.json({ ok: true }));
export default app;`} />

One middleware call. Works with Express, Hono, Fastify, or raw fetch.

## 14-protocol matrix

| Protocol | Coverage | Chains |
|---|---|---|
| <ProtocolBadge protocolId="x402" /> | HTTP 402 + WWW-Authenticate handshake | <ChainBadge chainId="goat-mainnet" size="sm" /> <ChainBadge chainId="base-mainnet" size="sm" /> <ChainBadge chainId="arbitrum-mainnet" size="sm" /> <ChainBadge chainId="optimism-mainnet" size="sm" /> |
| <ProtocolBadge protocolId="eip-3009" /> | Authorized USDC transfer | <ChainBadge chainId="base-mainnet" size="sm" /> <ChainBadge chainId="arbitrum-mainnet" size="sm" /> <ChainBadge chainId="polygon-mainnet" size="sm" /> |
| <ProtocolBadge protocolId="mpp" /> | Multi-Party split settlements | <ChainBadge chainId="goat-mainnet" size="sm" /> <ChainBadge chainId="base-mainnet" size="sm" /> |
| <ProtocolBadge protocolId="stellar-channels" /> | Sub-cent micropayments | <ChainBadge chainId="stellar-mainnet" size="sm" /> |
| <ProtocolBadge protocolId="wormhole-ntt" /> | Native Token Transfers | <ChainBadge chainId="base-mainnet" size="sm" /> <ChainBadge chainId="solana-mainnet" size="sm" /> <ChainBadge chainId="sui-mainnet" size="sm" /> |
| <ProtocolBadge protocolId="circle-gateway" /> | USDC mint/burn via Circle | <ChainBadge chainId="base-mainnet" size="sm" /> <ChainBadge chainId="solana-mainnet" size="sm" /> |
| <ProtocolBadge protocolId="rlusd" /> | Ripple USD stablecoin | <ChainBadge chainId="xrpl-mainnet" size="sm" /> <ChainBadge chainId="base-mainnet" size="sm" /> |
| <ProtocolBadge protocolId="xrpl-x402" /> | XRPL-native MPT paywall | <ChainBadge chainId="xrpl-mainnet" size="sm" /> |
| <ProtocolBadge protocolId="erc-8004" /> | Agent identity + reputation | <ChainBadge chainId="base-mainnet" size="sm" /> <ChainBadge chainId="celo-mainnet" size="sm" /> |
| <ProtocolBadge protocolId="aave-v3" /> | Idle USDC treasury yield | <ChainBadge chainId="base-mainnet" size="sm" /> <ChainBadge chainId="arbitrum-mainnet" size="sm" /> |
| <ProtocolBadge protocolId="ows" /> | Vault-bound signing | all 27 chains |
| <ProtocolBadge protocolId="peg-btc" /> | PegBTC lending / JIT USDC | <ChainBadge chainId="goat-mainnet" size="sm" /> |
| <ProtocolBadge protocolId="flare-fxrp" /> | Bridged XRP gas + asset | <ChainBadge chainId="flare-mainnet" size="sm" /> |
| <ProtocolBadge protocolId="initia-vip" /> | Validator-incentive USDC settle | <ChainBadge chainId="initia-mainnet" size="sm" /> |

## Sell an API as a paid MCP server

<CodeBlock language="ts" caption="paid-mcp-server.ts" code={`import { createPaidMcpServer, paidTool } from 'n-payment/agent';

const server = createPaidMcpServer({
  name:  'MyAgentService',
  payTo: '0xYourAddress',
  chain: 'goat-mainnet',
  tools: [
    paidTool({
      name:        'forecast',
      description: 'Get weather forecast',
      price:        10_000n,                  // $0.01 USDC
      handler:      async ({ city }) => ({ city, temp: 22 }),
    }),
  ],
});

await server.listen(3000);`} />

## Verify against npm

<CodeBlock language="bash" code={`npm view n-payment version    # → 0.29.1
npm view n-payment dependencies   # → only 'viem'`} />
