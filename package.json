{
  "name": "hypermove-app",
  "version": "0.1.0",
  "private": true,
  "description": "hypermove.xyz — make any web3 dApp agent-callable in 3 lines of HTML. Funded under GOAT AI Builder Grants.",
  "scripts": {
    "dev": "next dev -p 3003",
    "build": "next build",
    "start": "next start -p 3003",
    "test": "vitest run --reporter=verbose",
    "test:watch": "vitest",
    "mcp:smoke": "vitest run tests/mcp-gateway.test.ts tests/mcp-route.test.ts",
    "mcp:refresh": "vitest run -t \"catalog\"",
    "report": "node scripts/report.mjs",
    "lint": "next lint",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@mdx-js/loader": "^3.1.1",
    "@mdx-js/react": "^3.1.1",
    "@modelcontextprotocol/sdk": "^1.26.0",
    "@next/mdx": "^16.2.9",
    "@tanstack/react-query": "^5.101.2",
    "exa-js": "2.16.0",
    "linkedom": "^0.18.12",
    "lucide-react": "^1.21.0",
    "mcp-handler": "^1.1.0",
    "motion": "^12.42.0",
    "n-payment": "^0.30.1",
    "next": "14.2.18",
    "nim-skill": "github:phamdat721101/nim-skill",
    "pg": "8.13.1",
    "porto": "^0.2.37",
    "react": "18.3.1",
    "react-dom": "18.3.1",
    "rehype-pretty-code": "0.14.5",
    "remark-gfm": "4.0.1",
    "shiki": "4.3.1",
    "viem": "^2.53.1",
    "wagmi": "^3.6.21",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@mdx-js/mdx": "3.1.1",
    "@testing-library/jest-dom": "6.4.6",
    "@testing-library/react": "16.0.0",
    "@types/mdx": "2.0.13",
    "@types/node": "20.14.0",
    "@types/pg": "8.11.10",
    "@types/react": "18.3.3",
    "@types/react-dom": "18.3.0",
    "@vitest/coverage-v8": "1.6.0",
    "autoprefixer": "10.4.19",
    "jsdom": "24.1.0",
    "postcss": "8.4.39",
    "tailwindcss": "3.4.4",
    "typescript": "5.4.5",
    "vitest": "1.6.0",
    "xrpl": "5.0.0"
  },
  "_dep_justifications": {
    "pg": "Node.js Postgres driver. Drives Supabase registry storage via Server Action in src/app/portal/actions.ts. Single peer; ~50KB; mature.",
    "@next/mdx": "Required for /docs/* MDX content. Official Next.js plugin.",
    "remark-gfm": "Enables GitHub-Flavored-Markdown tables/strikethrough/task-lists in @next/mdx's remark pipeline (next.config.mjs) — without it, pipe-table syntax in /docs/*.mdx compiles as literal paragraph text, not a <table>. Used at Next.js build time (next.config.mjs), so it must be a real dependency, not devDependency.",
    "rehype-pretty-code": "Adds Shiki-based syntax highlighting to fenced code blocks across /docs/*.mdx via @next/mdx's rehype pipeline (next.config.mjs). Build-time dependency, same reasoning as remark-gfm.",
    "shiki": "Peer of rehype-pretty-code — the actual highlighting engine (TextMate grammars, VS Code themes). Build-time dependency.",
    "@testing-library/react": "Required for component tests under /tests/components. Industry standard.",
    "xrpl": "n-payment@0.30.1's optional peer dep for XRPL support (>=4.0.0, lazy-imported — see node_modules/.pnpm/n-payment.../package.json peerDependenciesMeta). Only exercised by scripts/demo-t54-rlusd-dream-cycle.ts's RLUSD_DEMO_MODE=live path (real testnet Payment/TrustSet submission + ledger validation) and its test double; the Next.js app itself never imports it. Exact-pinned devDependency, not bundled into any route."
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "pnpm": {
    "overrides": {
      "@xrplf/isomorphic>@noble/hashes": "1.8.0",
      "ripple-address-codec>@scure/base": "1.2.6",
      "ripple-keypairs>@noble/curves": "1.9.7",
      "xrpl>@scure/bip32": "1.7.0"
    }
  },
  "_pnpm_overrides_justifications": {
    "@xrplf/isomorphic>@noble/hashes": "2026-08-12, docs/prd/2026-08-12-fix-rlusd-payment-rail-esm-crash.md. @xrplf/isomorphic@1.0.2 (a real dependency of xrpl@5.0.0, which n-payment's XRPL settlement path lazy-imports at runtime — see settleXrplRlusd()/settleXrplAlreadySubmitted() in src/lib/mcp/npayment-rails.ts) declares @noble/hashes: ^2.0.1, which always resolved to the newest 2.x line (2.2.0). @noble/hashes@2.x is published type:module with zero CJS build, and @xrplf/isomorphic's own bundled CJS output does a plain require() of it — a real Node module-resolution crash confirmed live against every XRPL settlement attempt regardless of proof correctness. SCOPED to only @xrplf/isomorphic's resolution (not a blanket override) because @scure/bip39@2.2.0 (used by n-payment's own wallet-signing path, transitively) pins @noble/hashes to an EXACT '2.2.0' and imports the '@noble/hashes/webcrypto.js' subpath, which only exists in 2.x's exports map — a blanket 1.8.0 override was tried first and broke that path with a DIFFERENT ERR_PACKAGE_PATH_NOT_EXPORTED crash, confirmed live via the RUN_LIVE_XRPL_TESTS=true Tier 2 test in tests/mcp-xrpl-settlement.test.ts. The scoped `pkg>dep` override syntax pins @noble/hashes only where @xrplf/isomorphic itself resolves it, leaving @scure/bip39's exact 2.2.0 pin (and everything else in the tree) untouched. Remove this override only once @xrplf/isomorphic ships a release that either re-pins to a compatible @noble/hashes range or fixes its own require()->import() call — verify via the same live Tier 2 test before removing.",
    "ripple-address-codec>@scure/base": "2026-08-12, same PRD, found live in production AFTER the @noble/hashes fix above was deployed — same bug class, different link in the same xrpl@5.0.0 dependency chain. ripple-address-codec@5.0.1 (a real, non-peer dependency of @xrplf/isomorphic@1.0.2, reached via XRPL address encode/decode — e.g. deriving a classic address from a seed during wallet signing) declares @scure/base: 2.2.0 (published type:module, zero CJS build — same ESM-only migration pattern as @noble/hashes@2.x). The live crash: `require() of ES Module .../@scure+base@2.2.0/.../index.js from .../ripple-address-codec@5.0.1/.../xrp-codec.js not supported`. Pinned to @scure/base@1.2.6 (already present in the lockfile, confirmed CJS via main:'./lib/index.js' with no type:module, and already the version other consumers in this tree resolve without issue) — scoped only to ripple-address-codec's own resolution to avoid repeating the earlier blanket-override mistake.",
    "ripple-keypairs>@noble/curves": "2026-08-12, same PRD, fixed preemptively alongside the @scure/base fix above rather than waiting for a third live crash: ripple-keypairs@3.0.0 (used for XRPL seed-to-keypair derivation, i.e. wallet signing — the exact code path that was about to be exercised next) declares @noble/curves: 2.2.0 (same package.json shape as the other two: type:module, zero CJS build). Pinned to @noble/curves@1.9.7 (already present in the lockfile at a confirmed-CJS version, main:'index.js' with no type:module) — scoped only to ripple-keypairs's own resolution.",
    "xrpl>@scure/bip32": "2026-08-12, docs/feedback/2026-08-12-systemic-esm-cjs-dependency-pattern.md's predicted 4th instance, confirmed (not just predicted) by reading pnpm-lock.yaml directly: xrpl@5.0.0 itself (not a deep/optional transitive path) declares a real, non-peer dependency on @scure/bip32: 2.2.0, for HD-wallet (BIP-32) key derivation. @scure/bip32@2.2.0's own dependency block pins @noble/curves, @noble/hashes, and @scure/base ALL at 2.2.0 simultaneously — the identical type:module/zero-CJS-build pattern as the other three instances. Before scoping this override, grepped the entire lockfile for any OTHER consumer pinning @noble/curves, @noble/hashes, or @scure/base to an exact 2.2.0 (the exact class of check that caught the original @scure/bip39 conflict) — confirmed @scure/bip39@2.2.0 still independently pins @noble/hashes:2.2.0 exactly, so this override is SCOPED to xrpl>@scure/bip32 only (never a blanket @scure/bip32, @noble/hashes, @noble/curves, or @scure/base override). Pinned to @scure/bip32@1.7.0 — already present in the lockfile, confirmed CJS (used by viem's own bip32 dependency without issue), and itself resolves @noble/curves@1.9.7 + @noble/hashes@1.8.0 + @scure/base@1.2.6 — the exact same three CJS-compatible versions the other three overrides already pin, so this override cannot introduce a fifth divergent pin. Remove this override only once xrpl re-pins @scure/bip32 to a CJS-compatible range or ships a fixed build — verify via the same live Tier 2 test before removing."
  }
}
