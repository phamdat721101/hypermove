{
  "name": "hypermove-fce-extension",
  "version": "0.1.0",
  "description": "Flare Compute Extension (FCE) — TEE-node action handler bridging Flare's confidential-compute pipeline to HyperMove's MCP gateway.",
  "type": "module",
  "private": true,
  "license": "MIT",
  "scripts": {
    "dev": "tsx src/index.ts",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@hono/node-server": "2.0.11",
    "hono": "4.12.31",
    "viem": "2.55.4",
    "nim-skill": "github:phamdat721101/nim-skill"
  },
  "devDependencies": {
    "@types/node": "22.10.2",
    "tsx": "4.23.1",
    "typescript": "5.7.2",
    "vitest": "4.1.10"
  }
}
